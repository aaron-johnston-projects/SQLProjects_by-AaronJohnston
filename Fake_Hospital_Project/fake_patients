/* The following names and information was generated by using https://fakenamegenerator.com. Inserting the data into the table. If I'm importing the data into the database, I'd format it in Excel first then use SQL Server Studio Manager.
This time I had formatted it in excel then used a long formula with the & function.*/

/* The allergies was randomized using the following formula with a list of allergies =INDEX('data range',RANDBETWEEN(1,ROWS('data range')),1) */
